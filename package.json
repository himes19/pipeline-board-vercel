{
  "name": "pipeline-board-vercel",
  "version": "1.0.0",
  "description": "PulseVolt Pipeline Board — Vercel Serverless Deployment",
  "private": true,
  "engines": { "node": ">=18" },
  "dependencies": {
    "pg": "^8.13.0",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "vercel": "^39.0.0"
  },
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step needed'",
    "deploy": "vercel --prod"
  }
}
